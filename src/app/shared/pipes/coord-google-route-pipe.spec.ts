import { CoordGoogleRoutePipe } from './coord-google-route-pipe';

describe('CoordGoogleRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new CoordGoogleRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
