import { CoordGoogleMapsPipe } from './coord-google-maps-pipe';

describe('CoordGoogleMapsPipe', () => {
  it('create an instance', () => {
    const pipe = new CoordGoogleMapsPipe();
    expect(pipe).toBeTruthy();
  });
});
